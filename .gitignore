# 설치된 라이브러리 (용량이 커서 저장하지 않습니다. npm install 하면 다시 생깁니다)
node_modules/

# 빌드 결과물
frontend/dist/

# 방문자들이 찍은 사진과 기록 (개인정보라서 깃허브에 올리지 않습니다)
backend/data/uploads/
backend/data/photos.json

# 기타
.DS_Store
*.log
.env
.env.local
