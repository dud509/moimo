{
  "name": "moimo",
  "version": "1.0.0",
  "private": true,
  "description": "졸업전시 프로젝트 모이모 - 이름으로 캐릭터를 만들고 함께 사진 찍는 전시장 키오스크 웹사이트",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n 창고,화면 -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
